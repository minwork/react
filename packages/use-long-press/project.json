{
  "name": "use-long-press",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/use-long-press/src",
  "projectType": "library",
  "tags": [],
  "targets": {
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["packages/use-long-press/**/*.{ts,tsx,js,jsx}"]
      }
    },
    "build": {
      "executor": "@nrwl/vite:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "outputPath": "dist/packages/use-long-press"
      },
      "configurations": {
        "development": {
          "mode": "development"
        },
        "production": {
          "mode": "production"
        }
      }
    },
    "test": {
      "executor": "@nrwl/vite:test",
      "outputs": ["coverage/packages/use-long-press"],
      "options": {
        "passWithNoTests": true,
        "reportsDirectory": "../../coverage/packages/use-long-press"
      }
    },
    "release": {
      "executor": "nx:run-commands",
      "options": {
        "parallel": false,
        "commands": [
          {
            "command": "nx run use-long-press:test",
            "forwardAllArgs": false
          },
          {
            "command": "nx run use-long-press:build",
            "forwardAllArgs": false
          },
          {
            "command": "yarn run semantic-release --extends=./packages/use-long-press/release.config.js --no-ci",
            "forwardAllArgs": true
          }
        ]
      }
    }
  }
}
